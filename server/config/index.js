const config = {
  database: {
    DATABASE: 'CRM',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: 'localhost',
    timezone: 'Asia/Shanghai',
  },
};

module.exports = config;
